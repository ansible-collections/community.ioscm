---
plugin_routing:
  action:
    command:
      redirect: cisco.ioscm.ioscm
    config:
      redirect: cisco.ioscm.ioscm
    facts:
      redirect: cisco.ioscm.ioscm
  modules:
    command:
      redirect: cisco.ioscm.ioscm_command
    config:
      redirect: cisco.ioscm.ioscm_config
    facts:
      redirect: cisco.ioscm.ioscm_facts
requires_ansible: ">=2.9.10"
